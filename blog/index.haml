---
layout: blog-roll
---

%ul.posts
  == {% for post in site.posts %}
  %li.post
    %h1.post-title
      %a{:href => "{{ post.url }}"}
        == {{ post.title }}
    .post-excerpt
      %a{:href => "{{ post.url }}"}
        == {{ post.excerpt | markdownify }}
    .posted-date
      written
      %span.datetime.muted{:data => {:time => "{{ post.date }}"}}
        == {{ post.date | date_to_string }}
  == {% endfor %}
