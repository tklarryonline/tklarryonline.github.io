---
layout: blog-roll
---

%ul.posts
  == {% for post in site.posts %}
  %li.post
    .posted-date
      written
      %span.datetime.muted{:data => {:time => "{{ post.date }}"}}
        == {{ post.date | date_to_string }}
    %h1.post-title
      %a{:href => "{{ post.url }}"}
        == {{ post.title }}
    .post-excerpt
      == {{ post.excerpt }}
    %a.post-read-more{:href => "{{ post.url }}"}
      [ ... read more ... ]
  == {% endfor %}
